<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. Compose supplies the actual Material 3 ColorScheme at
         runtime (see ui/theme/Theme.kt); this XML theme only sets the platform window
         chrome so the app doesn't flash an unstyled system theme before Compose draws. -->
    <style name="Theme.WatchVault" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>
</resources>
